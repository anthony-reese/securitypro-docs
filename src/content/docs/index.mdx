---
title: Welcome to SecurityPro Docs
description: These guides provide everything you need to get started quickly and make the most of SecurityPro.
template: splash
hero:
  title: SecurityPro Documentation
  tagline: Everything you need to integrate and use SecurityPro APIs.
  actions:
    - text: Get Started
      link: /concepts/authentication
      icon: rocket
sidebar:
  label: Overview
  order: 1
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import { gettingStarted, guides, reference } from '../../data/sidebar.js';

## Getting Started
<CardGrid>
  {gettingStarted.map(item => (
    <Card title={item.label} icon={item.icon} href={item.href}>
      {item.desc}
    </Card>
  ))}
</CardGrid>

## Guides
<CardGrid>
  {guides.map(item => (
    <Card title={item.label} icon={item.icon} href={item.href}>
      {item.desc}
    </Card>
  ))}
</CardGrid>

## Reference
<CardGrid>
  {reference.map(item => (
    <Card title={item.label} icon={item.icon} href={item.href}>
      {item.desc}
    </Card>
  ))}
</CardGrid>

---

